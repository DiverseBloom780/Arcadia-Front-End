<UserControl x:Class="Arcadia.UI.Tabs.UpdaterTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#111">
    
    <Grid Margin="50">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🔄 Application Updater"
                   Foreground="#00D9FF"
                   FontSize="36"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,30">
            <TextBlock Text="Current Version: " 
                       Foreground="White"
                       FontSize="20"
                       VerticalAlignment="Center"/>
            
            <TextBlock x:Name="CurrentVersionText" 
                       Text="N/A" 
                       Foreground="#00D9FF"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Margin="5,0,30,0"
                       VerticalAlignment="Center"/>
            
            <Button Content="Check for Updates Now"
                    Click="CheckForUpdates_Click"
                    Background="#222"
                    Foreground="White"
                    FontSize="18"
                    Padding="15,8"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#333"/>
                                            <Setter Property="Foreground" Value="#00D9FF"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="#00D9FF"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,30">
            <TextBlock x:Name="StatusText" 
                       Text="Ready." 
                       Foreground="White"
                       FontSize="18"
                       Margin="0,0,0,15"/>

            <TextBlock x:Name="ReleaseInfoText" 
                       TextWrapping="Wrap" 
                       Foreground="#AAA"
                       FontSize="16"/>
        </StackPanel>
        
        <TextBlock Grid.Row="3" 
                   VerticalAlignment="Bottom"
                   HorizontalAlignment="Left"
                   Margin="0,0,0,20">
            <Hyperlink NavigateUri="https://github.com/DiverseBloom780/Arcadia-Front-End"
                       RequestNavigate="Hyperlink_RequestNavigate"
                       Foreground="#00D9FF">
                View Project Repository
            </Hyperlink>
        </TextBlock>
    </Grid>
</UserControl>