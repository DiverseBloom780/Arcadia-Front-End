<UserControl x:Class="Arcadia.UI.Tabs.NewsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FF0B0C10">
    <Grid Margin="46,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel>
            <Button DockPanel.Dock="Right" Content="REFRESH" Click="RefreshButton_Click"
                    Style="{StaticResource PremiumButtonStyle}" Padding="20,8"/>
            <TextBlock Text="DISCOVER" Foreground="White" FontSize="34" FontWeight="Bold"/>
        </DockPanel>
        <TextBlock x:Name="StatusText" Grid.Row="1" Text="Loading live deals and news..."
                   Foreground="#99FFFFFF" Margin="0,8,0,18"/>
        <TabControl x:Name="DiscoverTabs" Grid.Row="2" Background="Transparent" BorderThickness="0"
                    Foreground="White" FontSize="16">
            <TabItem Header="EPIC FREE GAMES">
                <ListBox x:Name="EpicList" Background="Transparent" BorderThickness="0"
                         MouseDoubleClick="Item_MouseDoubleClick">
                    <ListBox.ItemTemplate><DataTemplate>
                        <Border Style="{StaticResource GlassCardStyle}" Margin="0,0,0,10" Padding="16">
                            <StackPanel><TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="{Binding Subtitle}" Foreground="#00D9FF" Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="#CCFFFFFF" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate></ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
            <TabItem Header="GOG ON SALE">
                <ListBox x:Name="GogList" Background="Transparent" BorderThickness="0"
                         MouseDoubleClick="Item_MouseDoubleClick">
                    <ListBox.ItemTemplate><DataTemplate>
                        <Border Style="{StaticResource GlassCardStyle}" Margin="0,0,0,10" Padding="16">
                            <StackPanel><TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="{Binding Subtitle}" Foreground="#7CFF9B" Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="#CCFFFFFF" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate></ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
            <TabItem Header="UPCOMING &amp; NEWS">
                <ListBox x:Name="UpcomingList" Background="Transparent" BorderThickness="0"
                         MouseDoubleClick="Item_MouseDoubleClick">
                    <ListBox.ItemTemplate><DataTemplate>
                        <Border Style="{StaticResource GlassCardStyle}" Margin="0,0,0,10" Padding="16">
                            <StackPanel><TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="{Binding Subtitle}" Foreground="#FFB45E" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate></ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
            <TabItem Header="EMULATION PROGRESS">
                <ListBox x:Name="EmulationList" Background="Transparent" BorderThickness="0"
                         MouseDoubleClick="Item_MouseDoubleClick">
                    <ListBox.ItemTemplate><DataTemplate>
                        <Border Style="{StaticResource GlassCardStyle}" Margin="0,0,0,10" Padding="16">
                            <StackPanel><TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="{Binding Subtitle}" Foreground="#C28BFF" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate></ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
