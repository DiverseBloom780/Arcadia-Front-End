using System;
using System.IO;
namespace Arcadia.UI
{
    public class RomManager
    {
        public void AddRom(string romPath)
        {
            // Add the ROM to the ROM manager
        }

        public void RemoveRom(string romPath)
        {
            // Remove the ROM from the ROM manager
        }
    }
}
